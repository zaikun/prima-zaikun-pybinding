# test with equality constraints
# test with inequality constraints
# test by calling the minimize function directly to ensure that A_eq   is populated, and also with the wrapper
# test by calling the minimize function directly to ensure that A_ineq is populated, and also with the wrapper
# test with a single linear constraint
# test with multiple linear constraints
# test with scalar A, lb/ub
# test with single linear constraint and bounds